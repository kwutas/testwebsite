---
title-prefix: Programming Club
lang: en
dir: ltr
css-late: assets/style.css

author-meta: Programming Club
description-meta: Join us for weekly club meet-ups, professional development and networking opportunities, ongoing club projects, a thriving Discord community, and more!
robots-meta: noindex

# TODO: Allow setting url and image per page, also add og:site_name
url-og: programmingclub.com.au/testwebsite/
# TODO: Add structured properties for og:image
image-og: https://programmingclub.com.au/assets/photos/first_in_person.jpg
locale-og: en_AU

card-twitter: summary_large_image

header-includes: |
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Atkinson+Hyperlegible&display=swap">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous" defer></script>
    <script>
      function UpdateMode(dark) {
        document.getElementById("dark-mode-toggle").checked = dark;
      }
      function UpdateUrl(url, dark) {
        const urlObj = new URL(url);
        if (dark) {
          urlObj.searchParams.set("dark", "");
        } else {
          urlObj.searchParams.delete("dark");
        }
        return urlObj.href.replace("dark=", "dark");
      }
      function UpdateNavItems(dark) {
        const items = document.getElementsByClassName("nav-link");
        for (let item of items) {
          if (!item.ariaCurrent) {
            item.href = UpdateUrl(item.href, dark);
          }
        }
      }
      // TODO: Move media check to css
      // addEventListener("DOMContentLoaded", (event) => {
      //   const preferDark = matchMedia('(prefers-color-scheme: dark)').matches;
      //   const urlDark = new URLSearchParams(location.search).has("dark");
      //   if (urlDark) {
      //     UpdateNavItems(urlDark);
      //   }
      //   UpdateMode(preferDark || urlDark);
      //   matchMedia('(prefers-color-scheme: dark)').addEventListener('change', event => {
      //     UpdateMode(event.matches);
      //   });
      // });
      // TODO: Check for ?dark being removed via browser back and change to light mode
      // function ToggleDarkMode() {
      //   const dark = document.getElementById("dark-mode-toggle").checked;
      //   history.pushState({}, null, UpdateUrl(location, !dark));
      //   UpdateNavItems(!dark);
      // }
    </script>
---
